import React from 'react';

const AddModal = () => {
    return (
        <div>
            <h2>Add Car</h2>
            <form>
                <button type="submit">Add</button>
            </form>
        </div>
    );
};

export default AddModal;